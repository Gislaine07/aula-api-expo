import { StyleSheet } from 'react-native'; 

const styles = StyleSheet.create({
    container: {
        padding: 10,
    }, 
    tituloTela: {
        fontSize: 20, 
        fontWeight: 'bold', 
        marginBottom: 15, 
        width: '100%', 
        textAlign: 'center',
    },
    input: {
        borderWidth: 1,
        borderColor: '#1b0ed3',
        marginBottom: 10, 
        borderRadius: 15,
    }
});

export default styles;
